pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                sh '/home/jenkins/apache-maven-3.9.11/bin/mvn  -B -DskipTests clean package'
            }
        }
        stage('Test') {
            steps {
                sh '/home/jenkins/apache-maven-3.9.11/bin/mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Deliver') { 
            steps {
                sh './jenkins/scripts/deliver.sh' 
            }
        }
    }
}
